# Infrastructure Baseline

Infrastructure content is intentionally minimal in Commit 1.

Container definitions and orchestration files will be introduced only after component build surfaces exist.

Intended initial runtime set:
1. custody-api service
2. guildbot service
3. custody-portal service
4. postgres database

All services must run as separate containers in later commits.
